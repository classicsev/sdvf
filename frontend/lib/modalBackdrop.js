// Раньше клик по фону модалки закрывал её. Пользователь попросил убрать это
// поведение (2026-08-24) — случайный клик мимо окна создания/редактирования
// терял введённые данные. Модалки теперь закрываются только явно: кнопкой
// "Отмена"/крестиком/Esc или успешным сохранением — сам клик по фону ничего
// не делает.
//
// Функция оставлена (не убрана совсем), чтобы не трогать 20+ мест вызова
// {...backdropClickProps(() => setModalOpen(false))} по всему проекту —
// достаточно было обезвредить её здесь, в одном месте.
export function backdropClickProps(_onClose) {
  return {};
}
